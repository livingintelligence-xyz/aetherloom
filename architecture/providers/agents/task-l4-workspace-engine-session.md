# L4 — `WorkspaceEngineSession` and Real Local Composition

## Objective and dependency

Implement the production bridge seam specified by [01-workspace-engine-session.md](../01-workspace-engine-session.md) over real `LocalFolderStorageProvider`s and the L3 durable workspace. Begin only after the user confirms L3 merged and the merged default-branch SHA is verified. This is a high-risk standalone PR.

## Exact scope

- Replace the AppModel-facing nullable approval API with the exact non-optional `WorkspaceExecutionConfirmation` seam for executable preparations; add separate `LocalFolderDraft`/opaque `FolderAccessGrant` enrollment and reauthorization methods. Migrate `EngineSession`, `DemoEngineSession`, `WorkspaceEngineSession`, the minimum AppModel call sites, preview/test sessions, and compile/interaction tests together so the app build remains green.
- Implement `WorkspaceEngineSession` bootstrap, state reads/events, metadata-only location/sync-set operations, pause, conflict intent, ordinary prepare, **Review intentional deletions**, confirmation validation/internal `PlanApproval?` derivation, and sync-set deletion. The bridge never exposes or fabricates the opaque review authorization or core-owned execution reservation.
- Inject a folder-access resolver/scope lease abstraction; L4 tests use controlled capabilities. Keep actual `NSOpenPanel`, bookmark creation/resolution APIs, entitlements, and app default-session selection for L5.
- During enrollment, hold live access while canonicalizing the selected root and invoking L2's package-ancestry validator through the volume root; reject package roots, selections inside packages, and unreadable ancestor metadata. Then call `locationByRecordingVolumeIdentity`, commit identity/access references before provider construction, and enforce same/alias/ancestor overlap rejection.
- Compose `SyncOrchestrator`, real local providers, all file-backed stores, persistent stage, recovery state, and the manifest. Bootstrap reconstructs and presents but never auto-prepares or auto-executes.
- Hold security-scope leases over every authorized provider operation, including late indeterminate work and recovery.

Do not put sync decisions in bridge/app code, replay schedules, auto-confirm, add background sync, add real UI, or change the accepted package/metadata policy.

## Acceptance and focused tests

L4 solely owns [LW-03 through LW-08, LW-10 through LW-14, and LW-17](local-workspace-mvp.md#acceptance-matrix--one-owner-per-scenario). Use fake/call-log tests for metadata-only and access-failure proofs, real local providers only over test-created temporary directories for end-to-end cases, and reconstructed session instances for relaunch/recovery.

Focused coverage MUST include, for both Demo and Workspace sessions: a clear/no-count preparation that enables confirmation; a clear/nonzero-trash preparation that stays disabled until the exact trash acknowledgement; an approvable hold that requires exact trash/conflict acknowledgements and derives deterministic held `PlanApproval`; an ordinary non-approvable `massDeletion` hold with zero confirmation construction, zero enabled **Sync Now**, and zero executor calls; and a mixed hold containing ordinary `massDeletion` that remains non-approvable. Prove the exact threshold clamp matrix on construction, decode/preferences, sync-set creation, and update (`1...25`/`0.01...0.25` delete; `1...50`/`0.01...0.50` edit), with defaults as unexceedable maxima. Prove the pure gate receives an injected latch and returns a transition, while orchestrator/store calls durably load/apply it before preparation exposure; the latch survives relaunch and any allowed in-range increase, changed deletion evidence replaces/clears it, and partial/failed/unreconciled runs retain it until successful reconciled reviewed execution. Prove one-shot review issuance, exact-match authorization-to-reservation transition, and rejected transition in both safety activity and journal without bearer data; default expiries; reuse rejection; non-persistence across relaunch; exact sync-set/original-fingerprint/deletion-digest/counts/settings/world binding; fresh full prepare; semantic fingerprint reproducibility despite different generated decision/operation/run/preparation IDs, `generatedAt`/`scannedAt`, and enumeration order; semantic-drift sensitivity; mismatch failure; and distinct reviewed fingerprint. Then prove normal exact-count confirmation remains required and core atomically consumes the opaque live reservation bound to reviewed fingerprint/run/preparation before executor construction. Missing, expired, reused, relaunch-lost, wrong-fingerprint, wrong-run, and wrong-preparation reservations—including a reconstructed Codable reviewed hold—must return held/rejected with zero executor construction/calls. Also prove clear internal `nil` derivation only after confirmation validation; fingerprint/time/expiry/count mismatch; compile/source proof that AppModel sees no bearer review token/reservation, nullable/Boolean escape hatch, or path enrollment; all nine parameterized LW-08 causes; scope start/stop balance across success, refusal, cancellation, timeout, and indeterminate recovery; live-scope package-root and inside-package enrollment rejection plus unreadable ancestor failure; identity persistence before construction; overlap failure before scan; conflict-intent no-mutation; no schedule replay; metadata-only deletion; durable pause; and converged empty relaunch. Then run all bridge/core tests, the complete Swift suite, the app build, source/layering audits, and exact-head macOS/user validation.

## Finish and stop

Apply the shared [validation ladder](local-workspace-mvp.md#validation-ladder-for-l2l5). The durable orchestrator's [LW-20](local-workspace-mvp.md#acceptance-matrix--one-owner-per-scenario) evidence gate blocks L4. Finish only when every assigned row passes, no P0/P1 remains, and exact-head evidence is accepted. Freeze and stop for the user merge. Do not start L5.
